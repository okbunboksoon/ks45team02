<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ks45team02.ire.admin.mapper.DonationMapper">


	
	<select id="getDonationList" resultType="Donation" parameterType="String">
		<!-- 기부 조회 및 검색 -->
		SELECT 
			 donation_num as donationNum
			,user_id as userId
			,goods_co2_code as goodsC02Code
			,donation_amount as donationAmount
			,donation_date as donationDate
			,donation_status as donationStatus
		FROM 
			donation
		<where>
			<if test="searchKey != null and searchValue != ''">
				${searchKey} LIKE CONCAT('%', #{searchValue}, '%')
			</if>
			<if test="startDate != '' and startDate != null and endDate != '' and endDate != null">
				AND <![CDATA[donation_date >= #{startDate} AND donation_date <= #{endDate}]]>
			</if>	 
		</where>
	</select>












</mapper>